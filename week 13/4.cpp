#include <iostream>
using namespace std;

int matrix[4][3] = {{0, 0, 0},
                    {0, 1, 2},
                    {0, 0, 0},
                    {2, 1, 0}};

main()
{

} 
int identicalArrays ()
{
    for (int row = 0 ; row < 4 ; row ++)
    {
        for (int nextRow = 1 ; nextRow <4 ; nextRow++)
        {
           decision = checkIdentical (row , nextRow);
           if(descision == true)
           {
             if (row != nextRow)
           }

        }
    }
} 
bool checkIdentical(row , nextrow)
{ 
    for (int idx = 0 ; idx <3 ; idx++)
    {
        if(matrix[row][idx] != matrix[next][j])
        {
           
        }
    }

}                  